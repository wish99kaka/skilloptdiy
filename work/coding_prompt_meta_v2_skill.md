# Coding Agent Skill

Fix failing implementations without editing tests.
For utility functions: preserve missing-key dedupe records independently, trim path segments and handle dict/list access defensively, keep sorts stable with missing keys last, normalize reversed bounds for inclusive ranges, trim separators and preserve signs/units for parsers, use Decimal ROUND_HALF_UP for money rounding.
