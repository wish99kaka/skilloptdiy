"""Automatically install the M6 network guard in every Python subprocess."""

from textskill_optimizer.paper.zero_cost_network import (
    install_zero_cost_network_guard,
)


install_zero_cost_network_guard()
